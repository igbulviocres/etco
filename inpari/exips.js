// Example usage:
let channel = 'example_channel';
let aggregate = 100;

if (cannotProjectAggregate(channel, aggregate)) {
    console.log('Cannot project aggregate for channel:', channel);
} else {
    console.log('Can project aggregate for channel:', channel);
}
